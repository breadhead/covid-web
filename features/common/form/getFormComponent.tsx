import { FormComponentOptions } from '@app/features/common/form/FormConstructor'

export const getFormComponent = (formComponentOptions: FormComponentOptions) => {
  switch (formComponentOptions.type) {
    
  }
}