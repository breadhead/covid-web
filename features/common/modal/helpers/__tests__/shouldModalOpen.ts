// import { ModalState } from '../../reducer'
// import { shouldModalOpen } from '../shouldModalOpen'

// describe('shouldModalOpen', () => {
//   test('should return true if passed existing modal state', () => {
//     expect(shouldModalOpen(ModalState.login)).toBe(true)
//   })
//   test('should return false  if passed empty modal state', () => {
//     expect(shouldModalOpen(ModalState.empty)).toBe(false)
//   })
// })
